@@in# "// Config: str_strip_whitespace=true"
@@in# "name string"
@@in# "if m.name.len > 50 { return error("Validation Error: name length must be <= 50")"
@@in# "if m.name.len < 2 { return error("Validation Error: name length must be >= 2")"
@@in# "m.email = fn (v string) !string {"
@@in# "if '@' !in v {"
@@in# "return error('Invalid email')"
@@in# "fn (self User) display_name() string {"
@@in# "return '${self.name} <${self.email}>'"
